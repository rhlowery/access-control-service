apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  config.js: |
    window.ACS_CONFIG = {
      apiUrl: {{ .Values.config.apiUrl | quote }}
    };